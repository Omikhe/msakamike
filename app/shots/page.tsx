import { Camera } from "lucide-react"
export default function Shots() {
    return (
        <div className="flex items-center justify-center h-screen">
            <Camera size={300} className="-z-0 text-[#1f1f1f]"></Camera>

        </div>
    )
}